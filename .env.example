# Ollama is expected to be reachable from the machine running the app.
OLLAMA_BASE_URL=http://128.235.163.220:11434
OLLAMA_MODEL=glm-4.7-flash:latest

# Keep secrets here, never in git.
APP_ENV=development
APP_SECRET_KEY=change-me-before-deploy

# Optional future keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
